<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.icyclist.server.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="com.icyclist.server.model.Comment">
        <id property="id" column="comment_id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="comment_user_id"/>
        <result property="content" column="comment_content"/>
        <result property="createdAt" column="comment_created_at"/>
        <association property="user" javaType="com.icyclist.server.model.User">
            <id property="id" column="user_id"/>
            <result property="username" column="username"/>
            <result property="nickname" column="nickname"/>
            <result property="avatar" column="avatar"/>
        </association>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (post_id, user_id, content)
        VALUES (#{postId}, #{userId}, #{content})
    </insert>

    <select id="findByPostId" resultMap="CommentResultMap">
        SELECT
            c.id as comment_id,
            c.post_id,
            c.user_id as comment_user_id,
            c.content as comment_content,
            c.created_at as comment_created_at,
            u.id as user_id,
            u.username,
            u.nickname,
            u.avatar
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        WHERE c.post_id = #{postId}
        ORDER BY c.created_at ASC
    </select>

</mapper>




















